services:
  - type: web
    name: football-league
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: football-db
      mountPath: /data
      sizeGB: 1
